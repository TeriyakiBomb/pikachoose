Pikachoose has a bunch of options for choosing stuff in your document, for example:

Pick objects with the same fill, opacity, matching symbols, you can constrain by the current parent frame. You can also select hidden layers, unused symbols etc.

Just have a click around - if you're lucky I might even improve this readme. No promises tho.
